FILE(GLOB HEADER_FILES "CL/*.h")
FILE(GLOB HPP_FILES "CL/*.hpp")

install (FILES ${HEADER_FILES} DESTINATION "${CMAKE_INSTALL_PREFIX}/include/CL" COMPONENT beignet)
install (FILES ${HPP_FILES} DESTINATION "${CMAKE_INSTALL_PREFIX}/include/CL" COMPONENT beignet)
